## Install docker

``
cd docker
ansible-playbook playbook.yml -l localhost
``

## Install mysql

``
cd mysql
ansible-playbook playbook.yml -l localhost
``

## Run nginx

``
cd container1
ansible-playbook playbook.yml -l localhost
``