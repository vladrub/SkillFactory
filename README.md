## Install

```
ansible-galaxy install geerlingguy.docker # Установка роли
ansible-playbook docker.yml # установка docker на машинах группы app

ansible-galaxy install geerlingguy.postgresql # Установка роли
ansible-playbook postgres.yml # Установка postgres
```
